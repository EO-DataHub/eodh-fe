import type { Meta } from '@storybook/react';

import { LoadingInput } from './loading-input.component';

const meta: Meta<typeof LoadingInput> = {
  component: LoadingInput,
  title: 'libs/shared/design-system/input/LoadingInput',
};
export default meta;

export const Default = {};
