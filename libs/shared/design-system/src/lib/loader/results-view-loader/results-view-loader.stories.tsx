import type { Meta } from '@storybook/react';

import { ResultsViewLoader } from './results-view-loader';

const meta: Meta<typeof ResultsViewLoader> = {
  component: ResultsViewLoader,
  title: 'libs/shared/design-system/loader/ResultsViewLoader',
};
export default meta;

export const SampleResultsViewLoader = {
  args: {},
};
